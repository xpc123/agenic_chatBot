# 项目目标：通用可集成智能聊天机器人

> **一句话定位**: 30 分钟内让任何应用拥有生产级 AI 助手能力  
> **技术基座**: LangChain 1.0  
> **参考标杆**: GitHub Copilot、Cursor、ChatGPT

> 📖 **快速开始**：如果你是第一次使用，建议先阅读 [README.md](../README.md) 了解快速开始步骤和基础配置。

---

## 🎯 核心价值

### 项目愿景

**"Cursor 的强大功能 + 可集成性 = Agentic ChatBot"**

我们借鉴 Cursor 的核心能力（@路径引用、上下文理解、智能对话），但提供**开源、可集成、可定制**的解决方案，让任何产品都能拥有 Cursor 级别的 AI 助手能力。

### 解决什么问题？

| 痛点 | 现状 | 我们的方案 |
|------|------|-----------|
| **自建成本高** | 3-6 个月 + AI 专家团队 | 30 分钟 SDK 集成 / 开箱即用 |
| **定制性差** | Cursor/Copilot 无法深度定制，无法集成 | 完全可控的私有部署 + SDK 集成 |
| **集成困难** | Cursor 是 IDE，无法嵌入其他产品 | 即插即用 SDK + UI 组件 |
| **功能受限** | 现有开源方案功能单一（只有 RAG 或只有 Agent） | 完整功能：RAG + @路径引用 + MCP 工具 |

### 差异化优势

✅ **Cursor 级别的功能** - @路径引用、上下文理解、智能对话（参考 Cursor 设计）  
✅ **可集成性** - Cursor 做不到的：开源 SDK，可嵌入任何产品  
✅ **唯一同时支持独立使用和快速集成的开源方案**  
✅ **内置 MCP 工具生态** - 竞品需自建  
✅ **生产级工程实践** - 开箱即用的安全、监控、部署方案

### 与 Cursor 的对比

| 维度 | Cursor | Agentic ChatBot |
|------|-------|----------------|
| **@路径引用** | ✅ 支持（代码文件） | ✅ 支持（通用文件） |
| **上下文理解** | ✅ 强 | ✅ 强（参考 Cursor） |
| **智能对话** | ✅ 强 | ✅ 强 |
| **开源** | ❌ 闭源 | ✅ **开源** |
| **可集成** | ❌ 仅 IDE | ✅ **可集成到任何产品** |
| **可定制** | ❌ 无法定制 | ✅ **完全可定制** |
| **RAG 能力** | ⚠️ 有限 | ✅ 完整支持 |
| **MCP 工具** | ❌ 不支持 | ✅ 支持 |
| **使用场景** | 代码编辑 | **通用 AI 助手** |

**核心价值**：如果你想要 Cursor 的强大功能，但又需要集成到自己的产品中，Agentic ChatBot 是唯一的选择。

---

## 🔌 两种集成方式

本项目提供**两种完全不同的集成模式**，满足不同场景需求：

### 方式一：代码集成（SDK 调用）

**适用场景**: 产品需要在代码中调用 AI 能力，无需 UI 界面

```python
# 在你的产品代码中调用 SDK
from chatbot_sdk import ChatBot

# 初始化（配置产品上下文）
bot = ChatBot(
    rag_sources=["./docs", "./knowledge_base"],     # RAG 文档
    path_whitelist=["**/*.py", "config/**"],        # @路径白名单
    mcp_tools=["database", "email", "calculator"]   # MCP 工具
)

# 在业务逻辑中调用
def handle_customer_query(question: str):
    # AI 会自动加载产品上下文（RAG/@/MCP）
    response = bot.chat(question)
    return response

# 示例：后端 API
@app.post("/api/support")
async def support_endpoint(request: SupportRequest):
    answer = bot.chat(request.question, context=request.context)
    return {"answer": answer}
```

**特点**:
- ✅ 完全嵌入产品代码
- ✅ 灵活控制调用时机
- ✅ 适合无 UI 场景（API、后台任务、自动化脚本）
- ❌ 需要开发集成代码

---

### 方式二：独立 GUI（上下文连接）

**适用场景**: 产品需要开箱即用的聊天界面，无需编写代码

```json
// config.json - 只需配置产品上下文
{
  "context": {
    "rag_sources": [
      "./product_docs",
      "./user_manual.pdf"
    ],
    "path_whitelist": [
      "src/**/*.py",
      "config/**/*.yml"
    ],
    "mcp_servers": [
      "sqlite://./product.db",
      "http://api.yourproduct.com"
    ]
  }
}
```

```bash
# 启动 AI 助手（自带完整 GUI）
docker-compose up

# 访问 http://localhost:8000
# 用户可以直接在 GUI 中对话
# AI 自动加载配置的产品上下文
```

**特点**:
- ✅ 零代码集成（只需配置）
- ✅ 自带完整聊天界面
- ✅ 适合独立使用（客服、文档助手、内部工具）
- ✅ 支持嵌入 iframe（如需要）
- ❌ UI 定制能力有限（可通过主题配置调整）

---

### 📊 两种方式对比

| 维度 | 方式一：代码集成 (SDK) | 方式二：独立 GUI |
|------|----------------------|-----------------|
| **集成方式** | 调用 Python/JS SDK | 配置文件 + Docker 启动 |
| **是否需要编码** | ✅ 需要（集成代码） | ❌ 不需要（配置即可） |
| **UI 界面** | ❌ 无（你自己实现） | ✅ 自带完整界面 |
| **上下文加载** | 代码中指定 | 配置文件指定 |
| **使用场景** | API、后台任务、自动化 | 独立助手、客服系统 |
| **定制灵活性** | 🟢 极高 | 🟡 中等（主题配置） |
| **上手难度** | 🟡 中等（需开发） | 🟢 极低（配置即用） |
| **适合人群** | 开发者 | 产品经理、非技术人员 |

---

### 🔄 混合使用

两种方式可以**同时使用**：

```
产品架构示例：
┌─────────────────────────────────────────┐
│  你的产品                               │
│  ┌─────────────────┐  ┌──────────────┐ │
│  │  前端 Web 应用   │  │  后端 API    │ │
│  │                 │  │              │ │
│  │  嵌入 AI 助手    │  │  SDK 调用 ◄──┼─┤ 方式一：代码集成
│  │  iframe         │  │  (自动化)    │ │
│  └─────────────────┘  └──────────────┘ │
└─────────────────────────────────────────┘
          │
          ▼ 共享相同的上下文配置
┌─────────────────────────────────────────┐
│  AI 助手（独立部署）                     │ ◄── 方式二：独立 GUI
│  • RAG: 产品文档                        │
│  • @引用: 代码库                        │
│  • MCP: 数据库 + API                    │
└─────────────────────────────────────────┘
```

**实际案例**:
- **前端**: 嵌入 AI 助手 iframe，给用户提供实时帮助
- **后端**: SDK 调用 AI，自动处理工单、生成报告
- **内部**: 独立 GUI，技术支持团队使用

---

## 🏗️ 系统架构与数据流

### 系统架构总览图

```
┌─────────────────────────────────────────────────────────────────────────────────────────────┐
│                                    用户交互层 (User Interface Layer)                          │
├─────────────────────────────────────────┬───────────────────────────────────────────────────┤
│         独立GUI模式 (Standalone)          │              SDK集成模式 (Embedded)                │
│  ┌──────────────┐  ┌──────────────┐    │    ┌──────────────┐  ┌──────────────┐           │
│  │  Web UI      │  │ Desktop App  │    │    │ Python SDK   │  │ JavaScript   │           │
│  │  (React)     │  │ (Electron)   │    │    │              │  │ SDK          │           │
│  │              │  │              │    │    │              │  │              │           │
│  │ • 聊天界面   │  │ • 原生界面   │    │    │ • API调用    │  │ • Web集成    │           │
│  │ • 流式显示   │  │ • 系统集成   │    │    │ • 异步支持   │  │ • 组件化     │           │
│  └──────────────┘  └──────────────┘    │    └──────────────┘  └──────────────┘           │
│           │               │             │             │               │                    │
└───────────┼───────────────┼─────────────┴─────────────┼───────────────┼────────────────────┘
            │               │                           │               │
            └───────────────┴───────────────┬───────────┴───────────────┘
                                            ▼
┌─────────────────────────────────────────────────────────────────────────────────────────────┐
│                                   API网关层 (API Gateway Layer)                              │
│  ┌─────────────────────────────────────────────────────────────────────────────────────┐   │
│  │  FastAPI Server                                                                     │   │
│  │  • WebSocket支持 (流式输出)    • REST API    • 身份认证    • 请求限流              │   │
│  └─────────────────────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────────────────────┘
                                            ▼
┌─────────────────────────────────────────────────────────────────────────────────────────────┐
│                              上下文编排层 (Context Orchestration Layer)                       │
│  ┌─────────────────────────────────────────────────────────────────────────────────────┐   │
│  │                         上下文加载器 (Context Loader)                                │   │
│  │  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐  ┌──────────────┐  │   │
│  │  │  RAG 检索引擎   │  │  @路径引用系统  │  │  MCP工具注册表  │  │  会话记忆    │  │   │
│  │  │                 │  │                 │  │                 │  │              │  │   │
│  │  │ • 向量数据库    │  │ • 白名单验证    │  │ • 工具发现      │  │ • 短期记忆   │  │   │
│  │  │ • 语义检索      │  │ • 文件读取      │  │ • 动态注册      │  │ • 长期记忆   │  │   │
│  │  │ • 混合搜索      │  │ • 目录扫描      │  │ • 权限控制      │  │ • 摘要压缩   │  │   │
│  │  │ • 重排序        │  │ • 安全沙箱      │  │ • 参数验证      │  │ • 知识提取   │  │   │
│  │  └─────────────────┘  └─────────────────┘  └─────────────────┘  └──────────────┘  │   │
│  └─────────────────────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────────────────────┘
                                            ▼
┌─────────────────────────────────────────────────────────────────────────────────────────────┐
│                            智能体编排层 (Agent Orchestration Layer)                          │
│  ┌─────────────────────────────────────────────────────────────────────────────────────┐   │
│  │                   LangChain 1.0 Agent (create_agent + Middleware)                   │   │
│  │                                                                                     │   │
│  │  ┌──────────────────────────────────────────────────────────────────────────┐     │   │
│  │  │                        核心 Agent 循环 (Agent Loop)                       │     │   │
│  │  │  ┌─────────────┐     ┌─────────────┐     ┌─────────────┐                │     │   │
│  │  │  │  调用模型   │ ──▶ │  选择工具   │ ──▶ │  执行工具   │ ──▶ 循环/完成  │     │   │
│  │  │  │ (LLM Call)  │     │(Tool Select)│     │(Tool Execute)│                │     │   │
│  │  │  └─────────────┘     └─────────────┘     └─────────────┘                │     │   │
│  │  └──────────────────────────────────────────────────────────────────────────┘     │   │
│  │                                                                                     │   │
│  │  ┌──────────────────────────────────────────────────────────────────────────┐     │   │
│  │  │                         Middleware 中间件                                │     │   │
│  │  │  • PIIMiddleware (敏感信息过滤)     • SummarizationMiddleware (历史压缩) │     │   │
│  │  │  • HumanInTheLoopMiddleware (人工审批)   • 自定义 Middleware            │     │   │
│  │  └──────────────────────────────────────────────────────────────────────────┘     │   │
│  │                                                                                     │   │
│  │  ┌──────────────────────────────────────────────────────────────────────────┐     │   │
│  │  │              内置能力 (Built-in Features via LangGraph)                  │     │   │
│  │  │  • 持久化 (Persistence)  • 流式输出 (Streaming)  • 时间回溯 (Time Travel)│     │   │
│  │  └──────────────────────────────────────────────────────────────────────────┘     │   │
│  └─────────────────────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────────────────────┘
                                            ▼
┌─────────────────────────────────────────────────────────────────────────────────────────────┐
│                              工具执行层 (Tool Execution Layer)                               │
│  ┌──────────────────────┬──────────────────────┬──────────────────────┬─────────────────┐  │
│  │   MCP 工具集合       │   内置工具           │   自定义工具          │   外部API       │  │
│  │                      │                      │                      │                 │  │
│  │ • 数据库查询         │ • 代码执行器         │ • 业务逻辑工具        │ • REST API      │  │
│  │ • 文件操作           │ • 计算器             │ • 领域专用工具        │ • GraphQL       │  │
│  │ • 搜索引擎           │ • 日期时间           │ • 数据转换           │ • WebSocket     │  │
│  │ • 邮件服务           │ • 字符串处理         │ • 报表生成           │ • gRPC          │  │
│  │ • 第三方集成         │ • 格式转换           │ • 通知推送           │ • Webhook       │  │
│  └──────────────────────┴──────────────────────┴──────────────────────┴─────────────────┘  │
│  ┌─────────────────────────────────────────────────────────────────────────────────────┐   │
│  │                      工具沙箱 (Tool Sandbox)                                        │   │
│  │  • 权限隔离    • 资源限制    • 超时控制    • 异常捕获    • 日志记录                │   │
│  └─────────────────────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────────────────────┘
                                            ▼
┌─────────────────────────────────────────────────────────────────────────────────────────────┐
│                              LLM服务层 (LLM Service Layer)                                   │
│  ┌─────────────────────────────────────────────────────────────────────────────────────┐   │
│  │                         LLM 客户端 (Multi-Provider Support)                         │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │   │
│  │  │   OpenAI     │  │   Anthropic  │  │  本地模型    │  │   其他提供商  │          │   │
│  │  │              │  │              │  │              │  │              │          │   │
│  │  │ • GPT-4      │  │ • Claude 3   │  │ • Ollama     │  │ • Azure      │          │   │
│  │  │ • GPT-3.5    │  │ • Claude 2   │  │ • LLaMA      │  │ • Cohere     │          │   │
│  │  │ • Streaming  │  │ • Function   │  │ • Qwen       │  │ • Gemini     │          │   │
│  │  │ • Function   │  │   Calling    │  │ • ChatGLM    │  │              │          │   │
│  │  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘          │   │
│  │                                                                                     │   │
│  │  ┌──────────────────────────────────────────────────────────────────────────┐     │   │
│  │  │  智能路由 (Smart Routing)                                                │     │   │
│  │  │  • 模型选择    • 负载均衡    • 故障转移    • 成本优化    • 延迟优化     │     │   │
│  │  └──────────────────────────────────────────────────────────────────────────┘     │   │
│  └─────────────────────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────────────────────┘
                                            ▼
┌─────────────────────────────────────────────────────────────────────────────────────────────┐
│                              数据存储层 (Data Storage Layer)                                 │
│  ┌──────────────────────┬──────────────────────┬──────────────────────┬─────────────────┐  │
│  │   向量数据库         │   关系数据库          │   缓存数据库          │   文件存储      │  │
│  │                      │                      │                      │                 │  │
│  │ • ChromaDB           │ • PostgreSQL         │ • Redis              │ • 本地文件系统  │  │
│  │ • FAISS              │ • SQLite             │ • Memcached          │ • S3/OSS        │  │
│  │ • Pinecone           │ • MySQL              │                      │ • MinIO         │  │
│  │ • Weaviate           │                      │                      │                 │  │
│  │                      │                      │                      │                 │  │
│  │ 用途:                │ 用途:                │ 用途:                │ 用途:           │  │
│  │ • 文档向量           │ • 会话历史           │ • 会话缓存           │ • 原始文档      │  │
│  │ • 语义检索           │ • 用户数据           │ • 中间结果           │ • 上传文件      │  │
│  │ • 相似度搜索         │ • 工具配置           │ • 速率限制           │ • 日志文件      │  │
│  └──────────────────────┴──────────────────────┴──────────────────────┴─────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────────────────────┘
                                            ▼
┌─────────────────────────────────────────────────────────────────────────────────────────────┐
│                           基础设施层 (Infrastructure Layer)                                  │
│  ┌─────────────────────────────────────────────────────────────────────────────────────┐   │
│  │  监控与日志              │  部署与运维              │  安全与合规                    │   │
│  │  • 性能监控              │  • Docker容器            │  • 身份认证                    │   │
│  │  • 错误追踪              │  • K8s编排               │  • 权限控制                    │   │
│  │  • 日志聚合              │  • CI/CD流水线           │  • 数据加密                    │   │
│  │  • 指标收集              │  • 自动扩缩容            │  • 审计日志                    │   │
│  │  • 告警通知              │  • 蓝绿部署              │  • 合规检查                    │   │
│  └─────────────────────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────────────────────┘
```

### 核心数据流（Core Flow）

```
用户输入
   │
   ▼
┌─────────────────────────────────────────────────────┐
│  GUI / SDK                                          │
│  • Web 界面（独立使用）                              │
│  • 嵌入式组件（集成模式）                            │
└─────────────────────────────────────────────────────┘
   │
   ▼ 用户消息 + 上下文配置
┌─────────────────────────────────────────────────────┐
│  上下文加载器 (Context Loader)                       │
│  ┌─────────┬─────────┬─────────┐                   │
│  │ RAG检索 │@路径引用│MCP工具  │                   │
│  │         │         │         │                   │
│  │ 文档向量│本地文件 │外部数据 │                   │
│  │ 语义搜索│安全读取 │API调用  │                   │
│  └─────────┴─────────┴─────────┘                   │
│         ↓         ↓         ↓                       │
│      合并上下文（产品相关知识）                       │
└─────────────────────────────────────────────────────┘
   │
   ▼ 用户消息 + 产品上下文 + 工具定义
┌─────────────────────────────────────────────────────┐
│  AI Agent (LangChain 1.0 create_agent)              │
│  1. 理解意图（基于上下文）                           │
│  2. 选择工具（Tool Calling）                         │
│  3. 执行工具（MCP 协议）                             │
│  4. 生成回复（结合检索结果）                         │
└─────────────────────────────────────────────────────┘
   │
   ▼ 流式响应
┌─────────────────────────────────────────────────────┐
│  响应处理 & 记忆存储                                │
│  • 流式输出到 GUI                                   │
│  • 保存会话历史（短期/长期记忆）                     │
│  • 更新知识库（如需要）                              │
└─────────────────────────────────────────────────────┘
   │
   ▼
返回用户
```

## 🚀 核心能力矩阵

| 能力 | 优先级 | MVP | v1.0 | v2.0+ |
|------|-------|-----|------|-------|
| **Agent 规划** | P0 | ✅ 单轮对话 | ✅ 多步规划 | 🔄 自主反思 |
| **MCP 工具** | P0 | ✅ 1-2 个工具 | ✅ 动态注册 | 🔄 插件市场 |
| **会话记忆** | P0 | ✅ 短期记忆 | ✅ 长期记忆 | 🔄 知识图谱 |
| **RAG 检索** | P1 | ⏸️ | ✅ PDF/DOCX | ✅ @路径引用 |
| **Web UI** | P0 | ✅ 基础界面 | ✅ 流式响应 | 🔄 可嵌入组件 |
| **SDK** | P1 | ⏸️ | ✅ Python/JS | 🔄 多语言 |

> 图例: ✅ 已完成 | ⏸️ 不包含 | 🔄 规划中

---

## � LangChain 1.0 技术架构

本项目基于 **LangChain 1.0**（2025年发布）构建，使用最新的 Agent 架构和 Middleware 系统。

### 核心 API

```python
from langchain.agents import create_agent
from langchain.agents.middleware import (
    SummarizationMiddleware,
    PIIMiddleware,
    HumanInTheLoopMiddleware,
    ModelCallLimitMiddleware,
    ToolRetryMiddleware,
)
from langchain.tools import tool
from langchain.chat_models import init_chat_model

# 定义工具
@tool
def search_docs(query: str) -> str:
    """搜索文档库"""
    return f"搜索结果: {query}"

# 创建 Agent
agent = create_agent(
    model="gpt-4o",  # 支持自动推断提供商
    tools=[search_docs],
    system_prompt="你是一个有帮助的助手",
    middleware=[
        SummarizationMiddleware(model="gpt-4o-mini", trigger=("tokens", 4000)),
        PIIMiddleware("email", strategy="redact"),
        ModelCallLimitMiddleware(run_limit=10),
    ],
    checkpointer=InMemorySaver(),  # 自动持久化
)

# 执行
result = agent.invoke({"messages": [{"role": "user", "content": "你好"}]})
```

### 内置 Middleware（中间件）

LangChain 1.0 提供了丰富的内置中间件：

| 中间件 | 功能 | 使用场景 |
|--------|------|----------|
| **SummarizationMiddleware** | 自动压缩对话历史 | 长对话、节省 token |
| **PIIMiddleware** | 敏感信息过滤 | 合规要求、隐私保护 |
| **HumanInTheLoopMiddleware** | 人工审批高风险操作 | 数据库写入、邮件发送 |
| **ModelCallLimitMiddleware** | 限制模型调用次数 | 防止无限循环、成本控制 |
| **ToolRetryMiddleware** | 工具调用重试 | 处理临时失败 |
| **ModelRetryMiddleware** | 模型调用重试 | API 不稳定场景 |
| **ModelFallbackMiddleware** | 模型故障切换 | 高可用部署 |
| **ToolCallLimitMiddleware** | 限制工具调用次数 | 防止滥用 |
| **TodoListMiddleware** | 任务规划与跟踪 | 复杂多步任务 |
| **LLMToolSelectorMiddleware** | 智能工具选择 | 工具数量多时优化 |

### 自定义 Middleware

```python
from langchain.agents.middleware import AgentMiddleware, before_model, wrap_tool_call

# 方式一：类形式
class RAGContextMiddleware(AgentMiddleware):
    def before_model(self, state, runtime):
        # 注入 RAG 检索结果
        context_message = SystemMessage(content="相关文档: ...")
        messages = list(state.get("messages", []))
        messages.insert(0, context_message)
        return {"messages": messages}

# 方式二：装饰器形式
@before_model
def log_request(request):
    logger.info(f"Model request: {len(request.state['messages'])} messages")

@wrap_tool_call
def handle_tool_errors(request, handler):
    try:
        return handler(request)
    except Exception as e:
        return ToolMessage(content=f"工具错误: {e}", tool_call_id=request.tool_call["id"])
```

### Agent 执行流程

本项目采用**纯 ReAct (Reasoning + Acting) 模式**，LLM 自主决策是否使用工具：

```
用户输入
   │
   ▼
┌─────────────────────────────────────────┐
│  上下文构建 (Context Building)          │
│  • 会话历史                             │
│  • RAG 检索结果                         │
│  • 用户偏好                             │
└─────────────────────────────────────────┘
   │
   ▼
┌─────────────────────────────────────────┐
│  ReAct 循环 (最多 5 次迭代)              │
│  ┌───────────────────────────────────┐  │
│  │  1. LLM 思考 (Thought)            │  │
│  │     分析问题，决定下一步           │  │
│  │                                   │  │
│  │  2. 行动 (Action) - 可选           │  │
│  │     <tool_call>                   │  │
│  │     {"tool": "xxx", "args": {...}}│  │
│  │     </tool_call>                  │  │
│  │                                   │  │
│  │  3. 观察 (Observation)            │  │
│  │     工具执行结果                   │  │
│  │                                   │  │
│  │  4. 循环或给出 Final Answer       │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
   │
   ▼
┌─────────────────────────────────────────┐
│  Final Answer                           │
│  • 流式输出给用户                       │
│  • 保存会话历史                         │
└─────────────────────────────────────────┘
   │
   ▼
返回结果
```

#### ReAct 模式示例

```
用户: 列出 /tmp 目录的内容

Thought: 用户想查看 /tmp 目录的内容，我需要使用 list_directory 工具。

Action: <tool_call>{"tool": "list_directory", "args": {"path": "/tmp"}}</tool_call>

Observation: 目录包含 165 个子目录，主要是 UUID 格式的临时文件夹...

Thought: 我已经获得了目录内容，可以给用户完整的回复了。

Final Answer: /tmp 目录包含以下内容：
## 📂 子目录
该目录包含 165+ 个以 UUID 格式命名的临时子目录...
```

#### 为什么选择 ReAct 模式

| 特性 | 预判断模式 | ReAct 模式 |
|------|-----------|------------|
| **准确性** | 受规则限制 | ✅ LLM 自主决策 |
| **灵活性** | 固定流程 | ✅ 动态调整 |
| **可解释性** | 黑盒 | ✅ 显示推理过程 |
| **容错性** | 需手动处理 | ✅ 自动重试/调整 |
| **延迟** | ✅ 较快 | 需多次 LLM 调用 |

### 与 LangGraph 的关系

LangChain 1.0 的 `create_agent` 基于 LangGraph 构建，自动获得：

- ✅ **持久化** - 通过 `checkpointer` 自动保存会话状态
- ✅ **流式输出** - 支持 token 级别和步骤级别的流式
- ✅ **人工审批** - 支持暂停执行等待人工决策
- ✅ **时间回溯** - 可以回退到任意历史状态

### 模型初始化

```python
from langchain.chat_models import init_chat_model

# 方式一：模型标识符字符串（推荐）
model = init_chat_model("gpt-4o")  # 自动推断为 OpenAI
model = init_chat_model("claude-sonnet-4-5-20250929")  # 自动推断为 Anthropic
model = init_chat_model("openai:gpt-4o-mini")  # 显式指定提供商

# 方式二：完整配置
model = init_chat_model(
    "gpt-4o",
    temperature=0.5,
    max_tokens=4096,
    timeout=30,
)
```

### 参考资源

- 📖 [LangChain 1.0 官方文档](https://docs.langchain.com/oss/python/langchain/overview)
- 📖 [Agent 文档](https://docs.langchain.com/oss/python/langchain/agents)
- 📖 [Middleware 文档](https://docs.langchain.com/oss/python/langchain/middleware)
- 📖 [迁移指南](https://docs.langchain.com/oss/python/migrate/langchain-v1)
- 🔧 [GitHub 仓库](https://github.com/langchain-ai/langchain)

---

## �🔌 上下文加载详解（核心差异化）

AI 助手真正智能的关键在于**理解你的产品上下文**。我们支持三种上下文来源：

### 1️⃣ RAG 检索（文档知识库）

**适用场景**: 产品文档、知识库、FAQ、技术手册

```python
# 上传文档并自动索引
bot.upload_documents([
    "docs/user_manual.pdf",
    "docs/api_reference.md",
    "knowledge_base/*.docx"
])

# AI 自动检索相关内容
response = bot.chat("如何配置数据库连接？")
# AI 会自动从文档中找到答案并引用来源
```

**技术实现**:
- 文档解析：PDF、DOCX、Markdown、HTML
- 文本分块：智能分段，保留语义完整性
- 向量化：OpenAI Embeddings / BGE-M3
- 检索：语义相似度搜索 + 混合检索

---

### 2️⃣ @路径引用（本地文件/代码）

**适用场景**: 代码库、配置文件、日志文件

> 💡 **设计灵感**：参考 Cursor 的 @file 功能，但支持更通用的文件类型和集成场景。

```python
# 直接引用项目文件（类似 Cursor 的 @file）
response = bot.chat(
    "这个配置文件有什么问题？",
    context="@/config/database.yml"
)

# 引用整个目录（类似 Cursor 的 @folder）
response = bot.chat(
    "帮我审查这些 API 代码",
    context="@/src/api/"
)

# 在 SDK 中集成（Cursor 做不到的）
client.chat("分析这个文件", context="@/data/report.csv")
```

**安全机制**:
- ✅ 白名单限制（只能访问指定路径）
- ✅ 防止目录穿越攻击
- ✅ 文件类型验证
- ✅ 大小限制（避免加载超大文件）

**与 Cursor 的对比**：
- Cursor：仅支持代码文件，仅在 IDE 内使用
- Agentic ChatBot：支持所有文件类型，可集成到任何应用

---

### 3️⃣ MCP 工具（外部数据 & 业务逻辑）

**适用场景**: 数据库查询、API 调用、实时数据、业务操作

```python
# 注册 MCP 工具
@bot.register_mcp_tool()
def query_user_database(user_id: str):
    """从数据库查询用户信息"""
    return db.query("SELECT * FROM users WHERE id = ?", user_id)

@bot.register_mcp_tool()
def send_email(to: str, subject: str, body: str):
    """发送邮件通知"""
    return email_service.send(to, subject, body)

# AI 会自动调用这些工具
response = bot.chat("查询用户 123 的订单，并发邮件提醒他")
# AI 会：1) 调用 query_user_database  2) 调用 send_email
```

**MCP 协议优势**:
- 标准化工具接口（兼容其他支持 MCP 的系统）
- 动态工具发现与注册
- 权限控制与沙箱执行

---

### 📊 三种上下文的对比

| 维度 | RAG 检索 | @路径引用 | MCP 工具 |
|------|---------|---------|---------|
| **数据类型** | 静态文档 | 本地文件 | 实时数据 |
| **更新频率** | 低（手动上传） | 中（实时读取） | 高（API 调用） |
| **典型场景** | 知识库、FAQ | 代码审查、配置 | 数据库、业务操作 |
| **实现复杂度** | 中（需要向量化） | 低（文件读取） | 高（需开发工具） |
| **安全要求** | 低 | 高（路径验证） | 极高（权限+沙箱） |

### 🔄 组合使用示例

```python
# 实际场景：技术支持机器人
response = bot.chat(
    "用户反馈登录失败，帮我排查问题",
    context=[
        "@/logs/error.log",           # 本地日志文件
        "rag:troubleshooting_guide"   # 知识库文档
    ],
    tools=["query_user_status", "restart_service"]  # MCP 工具
)

# AI 处理流程：
# 1. 读取 error.log 查看错误信息
# 2. 从 troubleshooting_guide 检索解决方案
# 3. 调用 query_user_status 工具查询用户状态
# 4. 如需要，调用 restart_service 重启服务
# 5. 返回诊断结果和处理建议
```

---

## 📋 快速上手

根据你的需求选择合适的集成方式：

### 🎨 场景一：需要开箱即用的聊天界面 → 独立 GUI 模式

**零代码，只需配置**

```bash
# 1. 配置产品上下文
cat > config.json << EOF
{
  "context": {
    "rag_sources": ["./docs", "./manual.pdf"],
    "path_whitelist": ["src/**", "config/**"],
    "mcp_servers": ["sqlite://./data.db"]
  }
}
EOF

# 2. 启动 AI 助手
docker-compose up

# 3. 访问 http://localhost:8000
# ✅ 完整聊天界面已就绪，AI 自动加载你的产品上下文
```

**适合**: 产品文档助手、客服系统、内部工具

---

### 💻 场景二：在代码中调用 AI 能力 → SDK 集成模式

**需要编写集成代码**

```python
# 1. 安装 SDK
pip install agentic-chatbot

# 2. 初始化（配置产品上下文）
from chatbot_sdk import ChatBot

bot = ChatBot(
    rag_sources=["./docs"],               # RAG 文档
    path_whitelist=["**/*.py"],           # @路径白名单
    mcp_tools=["database", "calculator"]  # MCP 工具
)

# 3. 在业务代码中调用
def handle_customer_query(question: str):
    response = bot.chat(question)
    return response

# 示例：FastAPI 后端
@app.post("/api/support")
async def support_endpoint(request: SupportRequest):
    answer = bot.chat(request.question)
    return {"answer": answer}
```

**适合**: API 集成、自动化脚本、报告生成

---

### 🔀 场景三：两种方式同时使用 → 混合模式

```
你的产品架构：
┌─────────────────────────────────────────┐
│  前端: 嵌入 AI 助手 iframe (GUI 模式)    │
│  后端: SDK 调用自动化处理 (SDK 模式)     │
└─────────────────────────────────────────┘
          │
          ▼ 共享相同的上下文配置
┌─────────────────────────────────────────┐
│  AI 助手（统一部署）                     │
│  • RAG: 产品文档                        │
│  • @引用: 代码库                        │
│  • MCP: 数据库 + API                    │
└─────────────────────────────────────────┘
```

**实际案例**:
- **用户端**: 嵌入聊天窗口，提供实时帮助（GUI 模式）
- **后端**: 自动处理工单、生成报告（SDK 模式）
- **内部**: 技术支持团队独立使用（GUI 模式）
